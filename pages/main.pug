extends _layout
block header
    include header
block content       
    section.main.container
        div.box-post
            each item in posts               
                div.post
                    h3= item.titel
                    h4= item.user_id.username
                    a(href=`/post-user/${item._id}` class="read") read
